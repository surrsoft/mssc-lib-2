/**
 * Имена некоторых полей. Используется lodash-стиль, т.е. можно указывать вложенные поля используя точку (см. [tmwd])
 */
export enum EnJsonFieldName {
  EMAIL = 'email',
  USERNAME = 'username',
  PROFILE_ABOUT = 'profile.about',
  ROLES = 'roles'
}
